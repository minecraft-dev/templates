name: ${PLUGIN_NAME}
version: '${BUILD_COORDS.version}'
main: ${MAIN_CLASS}
#if (${API_VERSION})
api-version: '${API_VERSION}'
#end
#if (${LOG_PREFIX})
prefix: ${LOG_PREFIX}
#end
#if (${LOAD_AT})
load: ${LOAD_AT}
#end
#if (${AUTHORS})
authors: ${AUTHORS.toString(", ", "[", "]")}
#end
#if (${DESCRIPTION})
description: ${DESCRIPTION}
#end
#if (${WEBSITE})
website: ${WEBSITE}
#end
