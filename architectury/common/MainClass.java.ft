package ${MAIN_CLASS.packageName};

public final class ${MAIN_CLASS.className} {
    public static final String MOD_ID = "${MOD_ID}";

    public static void init() {
        // Write common init code here.
    }
}
